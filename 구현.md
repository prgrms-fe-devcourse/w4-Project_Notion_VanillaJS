- # 구현한 것
- #, ##, ### 입력받기
- 문서 제목 수정 및 삭제
- 자동완성 일부 구현(현재 입력된 키워드, 안녕하세요, 노션 클로닝, 데브코스, 프로그래머스);
- # 구현해야할 것
- 자동완성
    - autoComplete(이하 AC)를 안하는 경우 발생하는 문제 해결 못함
    - 라인의 첫 번째는 AC가 적용 안됨
    - 뛰어쓰기를 기준으로 AC 표시
    - 유저가 키워드 입력할 수 있게 UI 구성하기(EditorTools)
- 문서 링크
- 뒤로가기
- CSS 씌우기
- 문서 하위 문서 depth에 제한 걸기

- # 배운점 및 느낀점
- 변수명 지을때 너무 생각없이 지은 것 같다.
- 일전에 작은 로직도 함수로 쪼개서 코드 작성이 깔끔해져서 이번에도 시도했는데, 생각보다 무리하게 쪼갠 것 같다. DOM 태그들이 난무하니까 더러워보여서 함수를 쓰면 괜찮겠다 싶었는데 크게 효과는 없는 것 같다.
- 아이콘을 적용한 버튼의 클릭 이벤트는 버튼의 패딩부분에서 일어남
- position : fixed를 적용하면 z축으로 이동
- 커서 조작 방법이 정말정말정말정말정말너무너무너무너무너무 어렵다
- # 궁금증 및 배워야할 점
- textContent, innerHTML, innerText 구분과 태그
- 특정 태그의 자식 노드(childNodes)를 가져온 후, for of문을 돌릴 때 특정 인덱스는 제외되는 현상은 대체 뭘까?
- 태그 자신을 string으로 변환하는 방법은 없을까?
- css파일을 분리하는 기준은?
- DOM을 조작하는 문들을 좀 깔끔하게 정리하는 방법은 없을까?