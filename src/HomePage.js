import Navigation from './Nav.js'
import Header from './Header.js'

export default function HomePage({$target}){
    const nav = new Navigation({$target})
    const header = new Header({$target})

    this.render = () =>{

    }

}
